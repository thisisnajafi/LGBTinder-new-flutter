// Legacy hub — live settings is [SettingsPage] (PERF-DEDUP-003).
import '../../features/settings/pages/settings_page.dart';

export '../../features/settings/pages/settings_page.dart';

typedef ComprehensiveSettingsScreen = SettingsPage;
