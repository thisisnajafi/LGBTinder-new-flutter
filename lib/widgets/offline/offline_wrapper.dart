// Unused (unreachable). Live offline banner is
// lib/core/widgets/connectivity_banner.dart — overlay sibling so the child
// tree does not rebuild (PERF-COMP-SHARED-003). Not dual-maintained.

export '../../core/widgets/connectivity_banner.dart';
