# Deprecated: use android-ci.yml for full build pipeline.
# Kept for backward compatibility — delegates to the same triggers.
name: LGBTFinder Flutter App - Change Notification

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is superseded by android-ci.yml"
